[gd_resource type="Resource" load_steps=3 format=3]

[ext_resource type="Script" path="res://resources/gigs/gig_definition.gd" id="1"]
[ext_resource type="Script" path="res://resources/gigs/gig_requirement.gd" id="2"]

[sub_resource type="Resource" id="req1"]
script = ExtResource("2")
content = 2
state = 0
tags = 5
amount = 10
label = "Biometric Decrypted·Encrypted"

[resource]
script = ExtResource("1")
gig_name = "Encryption Job"
description = "GHOST_SIGNAL: My buyer wants re-encrypted data. Decrypt it, then lock it back up with fresh encryption. Don't ask why.\n\nDecrypt first, then run it through your new Encryptor — it needs a Key from top, just like the Decryptor."
order_index = 8
is_tutorial = true
requirements = Array[Resource]([SubResource("req1")])
reward_buildings = Array[String](["Encryptor"])
