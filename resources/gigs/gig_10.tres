[gd_resource type="Resource" load_steps=3 format=3]

[ext_resource type="Script" path="res://resources/gigs/gig_definition.gd" id="1"]
[ext_resource type="Script" path="res://resources/gigs/gig_requirement.gd" id="2"]

[sub_resource type="Resource" id="req1"]
script = ExtResource("2")
content = 1
state = 0
tags = 5
amount = 10
label = "Financial Decrypted·Encrypted"

[resource]
script = ExtResource("1")
gig_name = "Secure Transmission"
description = "CIPHER_QUEEN: That Financial stream of yours is ready for harder work. Decrypt it, then lock it back up with military-grade encryption.\n\nIf your Financial source and Key forge are still running, this builds on top. Decrypt first, then Encryptor — same logic you used on Biometric."
order_index = 10
is_tutorial = false
requirements = Array[Resource]([SubResource("req1")])
reward_buildings = Array[String]([])
