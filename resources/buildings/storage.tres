[gd_resource type="Resource" script_class="BuildingDefinition" load_steps=2 format=3]

[ext_resource type="Script" path="res://resources/buildings/building_definition.gd" id="1"]

[resource]
script = ExtResource("1")
building_name = "Storage"
description = "Buffers and stores data packets. Prevents data loss when downstream buildings are busy."
grid_size = Vector2i(2, 2)
color = Color(0, 1, 0.533, 1)
category = "storage"
base_cost = 75
