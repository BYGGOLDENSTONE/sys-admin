[gd_resource type="Resource" script_class="BuildingDefinition" load_steps=2 format=3]

[ext_resource type="Script" path="res://resources/buildings/building_definition.gd" id="1"]

[resource]
script = ExtResource("1")
building_name = "Splitter"
description = "Splits a single data flow into multiple outputs. Essential for parallel processing setups."
grid_size = Vector2i(1, 1)
color = Color(0.8, 0.8, 0.8, 1)
category = "routing"
base_cost = 60
visual_type = "splitter"
output_ports = Array[String](["right", "bottom"])
input_ports = Array[String](["left"])
